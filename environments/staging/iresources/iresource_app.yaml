apiVersion: apps/v1
kind: Deployment
metadata:
  name: iresource-app-client-staging
  namespace: staging
spec:
  selector:
    matchLabels:
      app: iresource-app-client-staging
  template:
    metadata:
      labels:
        app: iresource-app-client-staging
    spec:
      containers:
      - name: iresource-app-client-staging
        image: quyennv0602/iresource-client:dev-1.2.2
        imagePullPolicy: Always
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: iresource-app-client-staging
  namespace: staging
spec:
  selector:
    app: iresource-app-client-staging
  ports:
  - port: 80  
    targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: iresource-app-server-staging
  namespace: staging
spec:
  selector:
    matchLabels:
      app: iresource-app-server-staging
  template:
    metadata:
      labels:
        app: iresource-app-server-staging
    spec:
      containers:
      - name: iresource-app-server-staging
        image: quyennv0602/iresource-server:dev-1.0.5
        imagePullPolicy: Always
        env:
          - name: SPRING_DATASOURCE_URL
            value: jdbc:postgresql://postgres-postgresql:5432/postgres?currentSchema=iresource&binaryTransfer=true&reWriteBatchedInserts=true&stringtype=unspecified
          - name: SPRING_DATASOURCE_USERNAME
            value: postgres
          - name: SPRING_DATASOURCE_PASSWORD
            value: admin
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: iresource-app-server-staging
  namespace: staging
spec:
  selector:
    app: iresource-app-server-staging
  ports:
  - port: 8080
    targetPort: 8080



